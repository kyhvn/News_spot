# ============================================
# Core Dependencies
# ============================================
Flask==3.1.3
feedparser==6.0.12
requests==2.34.2
python-dotenv==1.0.1

# ============================================
# Additional Utilities
# ============================================
beautifulsoup4==4.12.3
lxml==5.2.2
cachetools==5.3.3
flask-compress==1.14

# ============================================
# Already installed (no need to install)
# ============================================
# click - already installed with Flask
# blinker - already installed with Flask
# markupsafe - already installed with Flask
# charset_normalizer - installed with requests
# idna - installed with requests
# urllib3 - installed with requests
# certifi - installed with requests